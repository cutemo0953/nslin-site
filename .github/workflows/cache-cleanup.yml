# Migrated 2026-04-26 to use shared reusable workflow at cutemo0953/.github@v1.

name: Cleanup Actions Storage

on:
  schedule:
    - cron: '0 3 * * 0'  # Sunday 3 AM UTC
  workflow_dispatch:

permissions:
  actions: write

jobs:
  cleanup:
    uses: cutemo0953/.github/.github/workflows/cache-cleanup.yml@v1
